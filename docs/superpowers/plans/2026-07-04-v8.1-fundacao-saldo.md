# V8.1 — Fundação do Design System SALDO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebrandar o CRM como SALDO: tokens/fontes, logo/favicon, primitivos de UI, e shell+sidebar+dashboard responsivos, a partir do Brand Kit.

**Architecture:** Tokens SALDO no Tailwind 4 `@theme` (cores + fontes self-hosted via `next/font`). Componente `LogoSaldo` (SVG) + favicon. Primitivos em `src/components/ui/`. Sidebar reestilizada com drawer mobile. Dashboard reusa os primitivos mantendo dados/RLS.

**Tech Stack:** Next.js 16 (App Router), Tailwind 4, `next/font/google`, TypeScript, Vitest (jsdom). Sem banco.

## Global Constraints

- **Só visual:** não alterar comportamento/funcionalidade nem RLS. Preservar acessibilidade (foco-visível, skip-link, `aria-current`).
- **Cores SALDO:** verde `#0FA968`, verde-claro `#3CD98A`, tinta `#0E1512`, tinta-2 `#111B16`, texto `#101614`, violeta `#7C5CFF`, creme `#F7F6F2`, cinza `#5A6163`, cinza-claro `#8C938E`, linha `#E7E4DC`, negativo `#C0503F`, borda-escura `#232A27`, mono-muted `#78827C`, texto-claro `#B4BCB6`.
- **Fontes:** Space Grotesk (`font-display`), IBM Plex Sans (`font-sans`, latin+latin-ext), IBM Plex Mono (`font-mono`).
- **Light-only** (a marca é clara). Sem migration.
- **Antes de commitar:** `npm run lint && npm run typecheck && npm test && npm run build`.
- **Alias** `@/*` → `./src/*`.

---

## File Structure

**Criar:**
- `src/components/marca/LogoSaldo.tsx` — logo (SVG + wordmark), variantes.
- `src/components/ui/{Card,Botao,Badge,PageHeader,StatCard}.tsx` — primitivos.
- `src/lib/ui/stat.ts` — `corValorStat` (puro).
- `src/tests/ui/stat.test.ts`, `src/tests/marca/logo.test.tsx`.

**Modificar:**
- `src/app/layout.tsx` — fontes SALDO + metadata.
- `src/app/globals.css` — tokens `@theme` + body/foco.
- `src/app/icon.svg` — favicon SALDO.
- `src/components/Sidebar.tsx` — rebrand + drawer mobile.
- `src/app/(app)/layout.tsx` — shell creme.
- `src/app/(app)/page.tsx` — dashboard reestilizado.

---

## Task 1: Fontes + tokens (fundação)

**Files:**
- Modify: `src/app/layout.tsx`
- Modify: `src/app/globals.css`

**Interfaces:**
- Produces: utilitárias Tailwind `bg-verde`/`text-tinta`/`border-linha`/… e `font-display`/`font-sans`/`font-mono`; body creme.

- [ ] **Step 1: Fontes no root layout**

Replace o topo de `src/app/layout.tsx` (imports + fontes + html):

```tsx
import type { Metadata, Viewport } from "next";
import { Space_Grotesk, IBM_Plex_Sans, IBM_Plex_Mono } from "next/font/google";
import "./globals.css";

const display = Space_Grotesk({ variable: "--font-space-grotesk", subsets: ["latin"], weight: ["500", "600", "700"] });
const sans = IBM_Plex_Sans({ variable: "--font-plex-sans", subsets: ["latin", "latin-ext"], weight: ["400", "500", "600"] });
const mono = IBM_Plex_Mono({ variable: "--font-plex-mono", subsets: ["latin"], weight: ["400", "500"] });

export const metadata: Metadata = {
  metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"),
  title: { default: "Saldo", template: "%s · Saldo" },
  description: "Saldo — CRM contábil all-in-one.",
  robots: { index: false, follow: false },
};

export const viewport: Viewport = { themeColor: "#0E1512" };

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="pt-BR" className={`${display.variable} ${sans.variable} ${mono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
```

- [ ] **Step 2: Tokens no globals.css**

Replace o conteúdo de `src/app/globals.css`:

```css
@import "tailwindcss";

/* Design system SALDO — light-only (a marca é clara). Tokens do Brand Kit. */
@theme {
  --color-verde: #0fa968;
  --color-verde-claro: #3cd98a;
  --color-tinta: #0e1512;
  --color-tinta-2: #111b16;
  --color-texto: #101614;
  --color-violeta: #7c5cff;
  --color-creme: #f7f6f2;
  --color-cinza: #5a6163;
  --color-cinza-claro: #8c938e;
  --color-linha: #e7e4dc;
  --color-negativo: #c0503f;
  --color-borda-escura: #232a27;
  --color-mono-muted: #78827c;
  --color-texto-claro: #b4bcb6;

  --font-display: var(--font-space-grotesk), sans-serif;
  --font-sans: var(--font-plex-sans), Arial, Helvetica, sans-serif;
  --font-mono: var(--font-plex-mono), ui-monospace, monospace;
}

body {
  background: var(--color-creme);
  color: var(--color-texto);
  font-family: var(--font-sans);
}

::selection {
  background: var(--color-verde);
  color: #fff;
}

/* Foco visível (WCAG 2.4.7) — verde da marca. */
:focus-visible {
  outline: 2px solid var(--color-verde);
  outline-offset: 2px;
  border-radius: 4px;
}
/* Sobre superfícies escuras (sidebar tinta), foco claro. */
.bg-tinta :focus-visible,
.bg-tinta-2 :focus-visible {
  outline-color: #ffffff;
}
```

- [ ] **Step 3: Verificar**

Run: `npm run lint && npm run typecheck && npm run build 2>&1 | grep -E "error TS|Failed|Compiled" | head`
Expected: sem erros; "Compiled successfully".

- [ ] **Step 4: Commit**

```bash
git add src/app/layout.tsx src/app/globals.css
git commit -m "feat(ui): tokens e fontes SALDO (Tailwind @theme + next/font) (V8.1)"
```

---

## Task 2: Logo + favicon

**Files:**
- Create: `src/components/marca/LogoSaldo.tsx`
- Modify: `src/app/icon.svg`
- Test: `src/tests/marca/logo.test.tsx`

**Interfaces:**
- Produces: `LogoSaldo({ variante?, tamanho?, className? })`; `variante: "claro" | "escuro" | "mono" | "simbolo"` (default `"claro"`); `tamanho` = px do símbolo (default 32).

- [ ] **Step 1: Smoke test que falha**

Create `src/tests/marca/logo.test.tsx`:

```tsx
import { describe, it, expect } from "vitest";
import { renderToStaticMarkup } from "react-dom/server";
import { LogoSaldo } from "@/components/marca/LogoSaldo";

describe("LogoSaldo", () => {
  it("renderiza o símbolo (svg) e o wordmark por padrão", () => {
    const html = renderToStaticMarkup(<LogoSaldo />);
    expect(html).toContain("<svg");
    expect(html).toContain("Saldo");
  });
  it("variante simbolo não traz o wordmark", () => {
    const html = renderToStaticMarkup(<LogoSaldo variante="simbolo" />);
    expect(html).toContain("<svg");
    expect(html).not.toContain("Saldo");
  });
});
```

- [ ] **Step 2: Rodar (falha)**

Run: `npm test -- src/tests/marca/logo.test.tsx`
Expected: FAIL — módulo não existe.

- [ ] **Step 3: Implementar o componente**

Create `src/components/marca/LogoSaldo.tsx`:

```tsx
type Variante = "claro" | "escuro" | "mono" | "simbolo";

// Símbolo: moeda (círculo) com seta pra cima (triângulo + haste). Geométrico, legível em 16px.
function Simbolo({ tamanho, fundo, marca }: { tamanho: number; fundo: string; marca: string }) {
  return (
    <svg width={tamanho} height={tamanho} viewBox="0 0 100 100" aria-hidden="true" focusable="false">
      {fundo !== "none" && <circle cx="50" cy="50" r="50" fill={fundo} />}
      {fundo === "none" && <circle cx="50" cy="50" r="47" fill="none" stroke={marca} strokeWidth="6" />}
      <polygon points="50,26 32,50 68,50" fill={marca} />
      <rect x="44" y="48" width="12" height="26" rx="3" fill={marca} />
    </svg>
  );
}

export function LogoSaldo({
  variante = "claro",
  tamanho = 32,
  className,
}: {
  variante?: Variante;
  tamanho?: number;
  className?: string;
}) {
  // fundo do círculo × cor da seta × cor do wordmark
  const cfg = {
    claro: { fundo: "#0FA968", marca: "#ffffff", texto: "#101614" },
    escuro: { fundo: "#0FA968", marca: "#ffffff", texto: "#F1F3F0" },
    mono: { fundo: "none", marca: "#ffffff", texto: "#ffffff" },
    simbolo: { fundo: "#0FA968", marca: "#ffffff", texto: "" },
  }[variante];

  return (
    <span className={`inline-flex items-center gap-2 ${className ?? ""}`}>
      <Simbolo tamanho={tamanho} fundo={cfg.fundo} marca={cfg.marca} />
      {variante !== "simbolo" && (
        <span
          className="font-display font-bold tracking-tight"
          style={{ fontSize: Math.round(tamanho * 0.72), color: cfg.texto }}
        >
          Saldo
        </span>
      )}
    </span>
  );
}
```

- [ ] **Step 4: Favicon SALDO**

Replace `src/app/icon.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="7" fill="#0FA968" />
  <polygon points="16,8 10,16 22,16" fill="#ffffff" />
  <rect x="13.5" y="15" width="5" height="9" rx="1.5" fill="#ffffff" />
</svg>
```

- [ ] **Step 5: Rodar (passa)**

Run: `npm test -- src/tests/marca/logo.test.tsx`
Expected: PASS (2).

- [ ] **Step 6: Commit**

```bash
git add src/components/marca/LogoSaldo.tsx src/app/icon.svg src/tests/marca/logo.test.tsx
git commit -m "feat(ui): LogoSaldo (SVG, variantes) + favicon SALDO (V8.1)"
```

---

## Task 3: Primitivos de UI

**Files:**
- Create: `src/lib/ui/stat.ts`
- Create: `src/components/ui/Card.tsx`, `Botao.tsx`, `Badge.tsx`, `PageHeader.tsx`, `StatCard.tsx`
- Test: `src/tests/ui/stat.test.ts`

**Interfaces:**
- Produces:
  - `corValorStat(v: VarianteStat): string` — `VarianteStat = "neutro" | "positivo" | "destaque" | "negativo"`.
  - `Card`, `Botao` (`variante?: "primario"|"secundario"|"fantasma"|"perigo"`), `Badge` (`variante?: "neutro"|"positivo"|"atencao"|"negativo"|"ia"`), `PageHeader` (`titulo`, `subtitulo?`, `acoes?`), `StatCard` (`rotulo`, `valor`, `variante?: VarianteStat`).

- [ ] **Step 1: Teste do helper (falha)**

Create `src/tests/ui/stat.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { corValorStat } from "@/lib/ui/stat";

describe("corValorStat", () => {
  it("mapeia variante → classe de cor", () => {
    expect(corValorStat("positivo")).toBe("text-verde");
    expect(corValorStat("destaque")).toBe("text-violeta");
    expect(corValorStat("negativo")).toBe("text-negativo");
    expect(corValorStat("neutro")).toBe("text-texto");
  });
});
```

- [ ] **Step 2: Rodar (falha)**

Run: `npm test -- src/tests/ui/stat.test.ts`
Expected: FAIL.

- [ ] **Step 3: Helper**

Create `src/lib/ui/stat.ts`:

```ts
export type VarianteStat = "neutro" | "positivo" | "destaque" | "negativo";

export function corValorStat(v: VarianteStat): string {
  return { neutro: "text-texto", positivo: "text-verde", destaque: "text-violeta", negativo: "text-negativo" }[v];
}
```

- [ ] **Step 4: Rodar (passa)**

Run: `npm test -- src/tests/ui/stat.test.ts`
Expected: PASS.

- [ ] **Step 5: Componentes**

Create `src/components/ui/Card.tsx`:

```tsx
export function Card({ children, className }: { children: React.ReactNode; className?: string }) {
  return <div className={`rounded-2xl border border-linha bg-white p-5 ${className ?? ""}`}>{children}</div>;
}
```

Create `src/components/ui/Botao.tsx`:

```tsx
import type { ButtonHTMLAttributes } from "react";

type Variante = "primario" | "secundario" | "fantasma" | "perigo";

const ESTILO: Record<Variante, string> = {
  primario: "bg-verde text-white hover:brightness-95",
  secundario: "border border-linha bg-white text-texto hover:bg-creme",
  fantasma: "text-texto hover:bg-creme",
  perigo: "bg-negativo text-white hover:brightness-95",
};

export function Botao({
  variante = "primario",
  className,
  ...props
}: ButtonHTMLAttributes<HTMLButtonElement> & { variante?: Variante }) {
  return (
    <button
      {...props}
      className={`inline-flex items-center justify-center gap-1 rounded-lg px-4 py-2 text-sm font-medium transition disabled:opacity-60 ${ESTILO[variante]} ${className ?? ""}`}
    />
  );
}
```

Create `src/components/ui/Badge.tsx`:

```tsx
type Variante = "neutro" | "positivo" | "atencao" | "negativo" | "ia";

const ESTILO: Record<Variante, string> = {
  neutro: "bg-creme text-cinza",
  positivo: "bg-verde/10 text-verde",
  atencao: "bg-amber-100 text-amber-800",
  negativo: "bg-negativo/10 text-negativo",
  ia: "bg-violeta/10 text-violeta",
};

export function Badge({ children, variante = "neutro" }: { children: React.ReactNode; variante?: Variante }) {
  return <span className={`inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium ${ESTILO[variante]}`}>{children}</span>;
}
```

Create `src/components/ui/PageHeader.tsx`:

```tsx
export function PageHeader({
  titulo,
  subtitulo,
  acoes,
}: {
  titulo: string;
  subtitulo?: string;
  acoes?: React.ReactNode;
}) {
  return (
    <div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
      <div>
        <h1 className="font-display text-2xl font-bold tracking-tight text-texto">{titulo}</h1>
        {subtitulo && <p className="mt-0.5 text-sm text-cinza">{subtitulo}</p>}
      </div>
      {acoes && <div className="flex flex-wrap gap-2">{acoes}</div>}
    </div>
  );
}
```

Create `src/components/ui/StatCard.tsx`:

```tsx
import { Card } from "./Card";
import { corValorStat, type VarianteStat } from "@/lib/ui/stat";

export function StatCard({
  rotulo,
  valor,
  variante = "neutro",
}: {
  rotulo: string;
  valor: string | number;
  variante?: VarianteStat;
}) {
  return (
    <Card className="p-4">
      <div className="font-mono text-xs text-cinza-claro">{rotulo}</div>
      <div className={`mt-1.5 font-display text-2xl font-semibold ${corValorStat(variante)}`}>{valor}</div>
    </Card>
  );
}
```

- [ ] **Step 6: Verificar**

Run: `npm run lint && npm run typecheck && npm test 2>&1 | grep "Tests "`
Expected: sem erros; testes passam.

- [ ] **Step 7: Commit**

```bash
git add src/lib/ui/stat.ts src/components/ui src/tests/ui/stat.test.ts
git commit -m "feat(ui): primitivos SALDO (Card/Botao/Badge/PageHeader/StatCard) (V8.1)"
```

---

## Task 4: Sidebar SALDO + drawer mobile

**Files:**
- Modify: `src/components/Sidebar.tsx`

**Interfaces:**
- Consumes: `LogoSaldo` (Task 2). Mantém a mesma assinatura `Sidebar({ papel, nome })` e a lógica de itens por papel.

- [ ] **Step 1: Reescrever a Sidebar**

Replace `src/components/Sidebar.tsx`:

```tsx
"use client";
import { useState } from "react";
import Link from "next/link";
import { usePathname } from "next/navigation";
import type { Papel } from "@/lib/tipos";
import { podeGerenciarFinanceiro } from "@/lib/financeiro/permissoes";
import { podeAtender } from "@/lib/clientes/permissoes";
import { sair } from "@/app/login/actions";
import { LogoSaldo } from "@/components/marca/LogoSaldo";

export function Sidebar({ papel, nome }: { papel: Papel; nome: string }) {
  const pathname = usePathname();
  const [aberto, setAberto] = useState(false);
  const itens = [
    { href: "/", label: "Início" },
    { href: "/clientes", label: "Clientes" },
    ...(podeAtender(papel) ? [{ href: "/atendimento", label: "Atendimento" }] : []),
    ...(podeGerenciarFinanceiro(papel) ? [{ href: "/financeiro/cadastros", label: "Financeiro" }] : []),
    ...(["admin", "assistente"].includes(papel) ? [{ href: "/integracoes/dominio", label: "Integração Domínio" }] : []),
    ...(papel === "admin" ? [{ href: "/usuarios", label: "Usuários" }] : []),
  ];
  const ehAtivo = (href: string) => (href === "/" ? pathname === "/" : pathname === href || pathname.startsWith(`${href}/`));

  const nav = (
    <nav aria-label="Navegação principal" className="flex flex-col gap-1 text-sm">
      {itens.map((it) => {
        const ativo = ehAtivo(it.href);
        return (
          <Link
            key={it.href}
            href={it.href}
            aria-current={ativo ? "page" : undefined}
            onClick={() => setAberto(false)}
            className={`rounded-lg px-3 py-2 ${ativo ? "bg-verde font-medium text-white" : "text-texto-claro hover:bg-tinta-2"}`}
          >
            {it.label}
          </Link>
        );
      })}
    </nav>
  );

  return (
    <>
      {/* Topo mobile */}
      <div className="flex items-center justify-between bg-tinta px-4 py-3 md:hidden">
        <LogoSaldo variante="escuro" tamanho={26} />
        <button
          type="button"
          aria-label="Abrir menu"
          aria-expanded={aberto}
          onClick={() => setAberto((v) => !v)}
          className="rounded-lg p-2 text-texto-claro hover:bg-tinta-2"
        >
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
            <line x1="3" y1="6" x2="21" y2="6" /><line x1="3" y1="12" x2="21" y2="12" /><line x1="3" y1="18" x2="21" y2="18" />
          </svg>
        </button>
      </div>

      {/* Drawer mobile */}
      {aberto && (
        <div className="fixed inset-0 z-40 md:hidden" onClick={() => setAberto(false)}>
          <div className="absolute inset-0 bg-black/40" />
          <aside className="absolute left-0 top-0 flex h-full w-64 flex-col gap-4 bg-tinta p-4" onClick={(e) => e.stopPropagation()}>
            <LogoSaldo variante="escuro" tamanho={28} />
            <p className="truncate font-mono text-xs text-mono-muted">{nome}</p>
            {nav}
            <form action={sair} className="mt-auto">
              <button type="submit" className="rounded-lg px-3 py-2 text-sm text-texto-claro hover:bg-tinta-2">Sair</button>
            </form>
          </aside>
        </div>
      )}

      {/* Sidebar desktop */}
      <aside className="hidden flex-col gap-4 bg-tinta p-4 md:flex md:h-screen md:w-56 md:shrink-0">
        <LogoSaldo variante="escuro" tamanho={28} />
        <p className="truncate font-mono text-xs text-mono-muted">{nome}</p>
        {nav}
        <form action={sair} className="mt-auto">
          <button type="submit" className="rounded-lg px-3 py-2 text-sm text-texto-claro hover:bg-tinta-2 hover:text-white">Sair</button>
        </form>
      </aside>
    </>
  );
}
```

- [ ] **Step 2: Verificar**

Run: `npm run lint && npm run typecheck && npm run build 2>&1 | grep -E "error TS|Failed|Compiled" | head`
Expected: sem erros; "Compiled successfully".

- [ ] **Step 3: Commit**

```bash
git add src/components/Sidebar.tsx
git commit -m "feat(ui): sidebar SALDO (tinta + ativo verde) com drawer mobile (V8.1)"
```

---

## Task 5: Shell + Dashboard reestilizados

**Files:**
- Modify: `src/app/(app)/layout.tsx`
- Modify: `src/app/(app)/page.tsx`

**Interfaces:**
- Consumes: `PageHeader`, `StatCard`, `Card`, `Botao` (Task 3). Mantém dados/queries/RLS da home.

- [ ] **Step 1: Shell (fundo creme)**

In `src/app/(app)/layout.tsx`, troque o `<main>` para fundo creme e padding responsivo, e ajuste o skip-link às cores novas:

```tsx
  return (
    <div className="flex min-h-screen flex-col md:flex-row">
      <a
        href="#conteudo"
        className="sr-only focus:not-sr-only focus:absolute focus:left-2 focus:top-2 focus:z-50 focus:rounded focus:bg-white focus:px-3 focus:py-2 focus:text-texto focus:shadow"
      >
        Pular para o conteúdo
      </a>
      <Sidebar papel={perfil.papel} nome={perfil.nome} />
      <main id="conteudo" className="flex-1 bg-creme p-4 md:p-6">
        {children}
      </main>
    </div>
  );
```

- [ ] **Step 2: Dashboard com os primitivos**

In `src/app/(app)/page.tsx`, troque o cabeçalho e os cards de resumo. Substitua o bloco do `return (...)` inicial (o `<div className="flex items-center justify-between">...</div>` do título) por um `PageHeader`, e os cards de total/ativos/inativos por `StatCard`. Imports no topo:

```tsx
import { PageHeader } from "@/components/ui/PageHeader";
import { StatCard } from "@/components/ui/StatCard";
import { Card } from "@/components/ui/Card";
import { Botao } from "@/components/ui/Botao";
```

Cabeçalho (substituindo o `<h1>Início</h1>` + ações):

```tsx
      <PageHeader
        titulo="Início"
        subtitulo={`${nf.format(ativos)} clientes ativos · ${nf.format(total)} no total`}
        acoes={
          podeCriar ? (
            <Link href="/clientes/novo">
              <Botao variante="primario"><span aria-hidden="true">+ </span>Novo cliente</Botao>
            </Link>
          ) : undefined
        }
      />
```

Cards de resumo (substituindo os `CardResumo` de total/ativos/inativos):

```tsx
      <div className="grid grid-cols-1 gap-3 sm:grid-cols-3">
        <StatCard rotulo="Clientes" valor={nf.format(total)} />
        <StatCard rotulo="Ativos" valor={nf.format(ativos)} variante="positivo" />
        <StatCard rotulo="Inativos" valor={nf.format(inativos)} />
      </div>
```

Envolva as demais seções existentes ("por regime", "recentes") em `Card` (troque os contêineres antigos por `<Card> … </Card>`), mantendo o conteúdo/links. Se houver estados de erro (`erroResumo`/`erroRecentes`), preserve-os dentro do respectivo `Card`.

- [ ] **Step 3: Verificar**

Run: `npm run lint && npm run typecheck && npm test 2>&1 | grep "Tests " && npm run build 2>&1 | grep -E "error TS|Failed|Compiled" | head`
Expected: sem erros; testes passam; "Compiled successfully".

- [ ] **Step 4: Verificar no app (desktop + mobile)**

`npm run dev`. Logado, na **home**: sidebar escura com logo SALDO e "Início" ativo em verde; fundo creme; `StatCards` com números em Space Grotesk (ativos em verde); "Novo cliente" verde. Reduza a janela para largura de celular: aparece a barra superior com hambúrguer; o **drawer** abre/fecha e navega. Confira o foco-visível (Tab) e as acentuações (fontes carregadas).
Expected: identidade SALDO aplicada, responsivo, sem regressão funcional.

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/layout.tsx" "src/app/(app)/page.tsx"
git commit -m "feat(ui): shell creme + dashboard SALDO (primitivos, responsivo) (V8.1)"
```

---

## Task 6: Fechamento do V8.1

**Files:** —

- [ ] **Step 1: Suíte completa**

Run: `npm run lint && npm run typecheck && npm test && npm run build`
Expected: tudo verde; build ok.

- [ ] **Step 2: CHANGELOG, ROADMAP e memória**

Registrar a V8.1 no `CHANGELOG.md`; em `ROADMAP.md` marcar V8 como 🚧 e listar V8.1 ✅ + V8.2 (rollout) ⬜. Memória: design system SALDO (tokens `@theme`, fontes `next/font`, `LogoSaldo`, primitivos `src/components/ui/`, sidebar+drawer, dashboard), Brand Kit no repo, V8.2 = rollout às demais telas.

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md ROADMAP.md
git commit -m "docs: V8.1 — Fundação do design system SALDO entregue"
```

---

## Self-Review (feito na escrita)

**Cobertura do spec:** fontes+tokens (§4)→T1; logo+favicon (§5)→T2; primitivos (§6)→T3; sidebar+shell (§7)→T4/T5; dashboard (§8)→T5; testes (§9)→T2/T3. ✅

**Placeholders:** nenhum "TBD/TODO"; código real. As modificações da T5 (dashboard) indicam os blocos exatos a trocar (cabeçalho, cards, `Card` nas seções).

**Consistência de tipos:** `LogoSaldo` (T2) usado na Sidebar (T4); `corValorStat`/`VarianteStat` (T3) no `StatCard` (T3) e no dashboard (T5); `PageHeader`/`StatCard`/`Card`/`Botao` (T3) no dashboard (T5); nomes de token (`verde`/`tinta`/`creme`/`linha`/`texto-claro`/`tinta-2`/`negativo`/`violeta`) idênticos entre `@theme` (T1) e todos os consumidores. Fontes: variáveis `--font-space-grotesk`/`--font-plex-sans`/`--font-plex-mono` (T1 layout) casam com `--font-display/sans/mono` (T1 globals).
