# V10-A — LGPD — Plano de implementação

> **Para executores:** implemente tarefa a tarefa. Cada tarefa termina com verificação e commit.

**Objetivo:** conformidade LGPD — relatório de dados por titular (acesso + portabilidade), ROPA
pré-semeado, histórico de consentimento, e exclusão por **anonimização** que respeita a retenção fiscal.

**Arquitetura:** regras de retenção e anonimização em **libs puras testadas** (é onde um erro vira violação
legal); tabelas LGPD **admin-only**; anonimização é **irreversível** e registrada. Relatório em PDF (reusa
`converterPdfHtml`) + JSON.

**Stack:** Postgres/RLS, Next 16 Server Actions, Gotenberg, vitest.

## Restrições globais

- **A anonimização NUNCA apaga dado fiscal.** Só toca campos pessoais não-fiscais (e-mail, telefone,
  representante, usuários do portal). Razão social, CNPJ, títulos e NFS-e permanecem.
- **Anonimização é irreversível** — registrar quem, o quê e quando; sem desfazer.
- Tabelas LGPD: **RLS admin-only**. Consentimento gravado por **service_role** (o titular não forja o próprio).
- Regras críticas (retenção, o que é anonimizável) em **função pura testada**, não espalhadas em actions.
- Rodar `npm run lint && npm run typecheck && npm test && npm run build` antes de cada commit.

---

### Tarefa 1: Banco + RLS

**Arquivos:** Criar `supabase/migrations/0096_lgpd.sql`; modificar `supabase/tests/rls.test.sql`

- [ ] **Passo 1: Migration** (enums, 3 tabelas, colunas em `escritorio_config`, RLS admin-only)

```sql
-- V10-A: LGPD — ROPA, consentimento, solicitações do titular, retenção.
do $$ begin create type lgpd_base_legal as enum
  ('consentimento','contrato','obrigacao_legal','legitimo_interesse','protecao_credito','exercicio_direitos');
exception when duplicate_object then null; end $$;
do $$ begin create type lgpd_solic_tipo as enum ('acesso','exclusao');
exception when duplicate_object then null; end $$;
do $$ begin create type lgpd_solic_status as enum ('aberta','concluida');
exception when duplicate_object then null; end $$;

create table if not exists lgpd_tratamento (
  id uuid primary key default gen_random_uuid(),
  finalidade text not null,
  categorias text not null,
  base_legal lgpd_base_legal not null,
  retencao text,
  ativo boolean not null default true,
  ordem int not null default 0
);

create table if not exists lgpd_consentimento_evento (
  id uuid primary key default gen_random_uuid(),
  cliente_id uuid references clientes(id) on delete cascade,
  tipo text not null,
  concedido boolean not null,
  origem text,
  usuario_id uuid references usuarios(id),
  criado_em timestamptz not null default now()
);
create index if not exists ix_lgpd_consent_cliente on lgpd_consentimento_evento(cliente_id, criado_em desc);

create table if not exists lgpd_solicitacao_titular (
  id uuid primary key default gen_random_uuid(),
  cliente_id uuid references clientes(id) on delete set null,
  tipo lgpd_solic_tipo not null,
  status lgpd_solic_status not null default 'aberta',
  retido jsonb,
  anonimizado jsonb,
  criado_por uuid references usuarios(id) default auth.uid(),
  criado_em timestamptz not null default now(),
  concluido_em timestamptz
);

alter table escritorio_config add column if not exists retencao_meses int not null default 60;
alter table escritorio_config add column if not exists lgpd_encarregado text;

alter table lgpd_tratamento enable row level security;
alter table lgpd_consentimento_evento enable row level security;
alter table lgpd_solicitacao_titular enable row level security;

do $$ begin
  drop policy if exists lgpd_trat_all on lgpd_tratamento;
  create policy lgpd_trat_all on lgpd_tratamento for all to authenticated
    using (auth_papel() = 'admin') with check (auth_papel() = 'admin');

  -- Consentimento: admin lê; NINGUÉM insere pela app (service_role grava o evento).
  drop policy if exists lgpd_consent_sel on lgpd_consentimento_evento;
  create policy lgpd_consent_sel on lgpd_consentimento_evento for select to authenticated
    using (auth_papel() = 'admin');

  drop policy if exists lgpd_solic_all on lgpd_solicitacao_titular;
  create policy lgpd_solic_all on lgpd_solicitacao_titular for all to authenticated
    using (auth_papel() = 'admin') with check (auth_papel() = 'admin');
end $$;
```

- [ ] **Passo 2:** `npm run db:migrate` → `0096` aplicada.
- [ ] **Passo 3: Asserts de RLS** (`rls.test.sql`): contador/assistente/financeiro **não** leem
  `lgpd_tratamento`, `lgpd_solicitacao_titular` nem `lgpd_consentimento_evento` (0 linhas); assistente **não**
  cria tratamento (`insufficient_privilege`); **ninguém** (nem admin) insere em
  `lgpd_consentimento_evento` pela app (sem policy de INSERT); cliente do portal não vê nada. `npm run db:test`.
- [ ] **Passo 4: Commit**

```bash
git add supabase/migrations/0096_lgpd.sql supabase/tests/rls.test.sql
git commit -m "feat(lgpd): tabelas de ROPA, consentimento e solicitacoes do titular (RLS admin-only)"
```

---

### Tarefa 2: Regras puras (retenção + anonimização) + testes

**Arquivos:** Criar `src/lib/lgpd/retencao.ts`, `src/lib/lgpd/anonimizacao.ts`, `src/lib/lgpd/tratamentos-seed.ts`
e os testes em `src/tests/lgpd/`.

**Interfaces produzidas:**

```ts
// retencao.ts
export function dentroDaRetencao(dataSaidaIso: string | null, meses: number, hojeIso: string): boolean;
export type SinaisFiscais = { temNfse: boolean; temTitulo: boolean; temDocumento: boolean; temObrigacao: boolean };
export function vereditoRetencao(s: SinaisFiscais, dataSaidaIso: string | null, meses: number, hojeIso: string):
  { reter: boolean; motivo: string };

// anonimizacao.ts
export const MARCADOR = "[anonimizado]";
export const CAMPOS_CLIENTE_ANONIMIZAVEIS: string[];   // email, telefone, responsavel_nome, representante
export function anonimizarValor(campo: string): string | null; // representante(jsonb) -> null; texto -> MARCADOR
// NUNCA inclui razao_social, cpf_cnpj, inscricao_* — são fiscais/PJ.

// tratamentos-seed.ts
export const TRATAMENTOS_SEED: { finalidade: string; categorias: string; base_legal: string; retencao: string; ordem: number }[];
```

- [ ] **Passo 1: Testes primeiro**

```ts
import { describe, it, expect } from "vitest";
import { dentroDaRetencao, vereditoRetencao } from "@/lib/lgpd/retencao";
import { CAMPOS_CLIENTE_ANONIMIZAVEIS } from "@/lib/lgpd/anonimizacao";

describe("dentroDaRetencao", () => {
  it("cliente SEM data de saída está em atividade → retém", () => {
    expect(dentroDaRetencao(null, 60, "2026-07-15")).toBe(true);
  });
  it("saiu há menos que o prazo → retém", () => {
    expect(dentroDaRetencao("2024-01-01", 60, "2026-07-15")).toBe(true);
  });
  it("saiu há mais que o prazo → não retém por tempo", () => {
    expect(dentroDaRetencao("2019-01-01", 60, "2026-07-15")).toBe(false);
  });
  it("exatamente no limite do mês ainda retém", () => {
    expect(dentroDaRetencao("2021-07-15", 60, "2026-07-15")).toBe(true);
  });
});

describe("vereditoRetencao", () => {
  const nada = { temNfse: false, temTitulo: false, temDocumento: false, temObrigacao: false };
  it("tem título → retém o esqueleto fiscal", () => {
    expect(vereditoRetencao({ ...nada, temTitulo: true }, "2019-01-01", 60, "2026-07-15").reter).toBe(true);
  });
  it("nada fiscal e fora do prazo → libera", () => {
    expect(vereditoRetencao(nada, "2019-01-01", 60, "2026-07-15").reter).toBe(false);
  });
  it("cliente ativo → retém (ainda em relação)", () => {
    expect(vereditoRetencao(nada, null, 60, "2026-07-15").reter).toBe(true);
  });
});

describe("campos anonimizáveis", () => {
  it("inclui os pessoais e NUNCA os fiscais/PJ", () => {
    expect(CAMPOS_CLIENTE_ANONIMIZAVEIS).toContain("email");
    expect(CAMPOS_CLIENTE_ANONIMIZAVEIS).toContain("telefone");
    expect(CAMPOS_CLIENTE_ANONIMIZAVEIS).not.toContain("razao_social");
    expect(CAMPOS_CLIENTE_ANONIMIZAVEIS).not.toContain("cpf_cnpj");
  });
});
```

- [ ] **Passo 2:** `npm test -- lgpd` → FAIL. Implementar (datas em UTC; `dentroDaRetencao` compara o mês
  de saída + `meses` com hoje). `npm test -- lgpd` → PASS.
- [ ] **Passo 3: Commit**

```bash
git add src/lib/lgpd src/tests/lgpd
git commit -m "feat(lgpd): regras puras de retencao fiscal e anonimizacao + seed do ROPA"
```

---

### Tarefa 3: Relatório de dados por titular (PDF + JSON)

**Arquivos:** Criar `src/lib/lgpd/relatorio.ts`; `src/app/(app)/lgpd/actions.ts` (parte do relatório)

- [ ] **Passo 1: `relatorio.ts`** — `montarRelatorio(clienteId)` (server, `createAdminSupabase` — reúne de
  todas as fontes) devolve um objeto com seções: **Cadastro** (dados do cliente + representante),
  **Financeiro**, **Documentos**, **NFS-e**, **Títulos e baixas**, **E-mails**, **Comunicados**, **Acessos
  ao portal**, **Solicitações**. Mais `relatorioParaHtml(rel)` (HTML sanitizado, só data URIs) e
  `relatorioParaJson(rel)`.

- [ ] **Passo 2: `actions.ts`** — `gerarRelatorioTitular(clienteId, formato: "pdf" | "json")`:
  - gate admin;
  - monta o relatório; se `pdf`, `converterPdfHtml(relatorioParaHtml(...))` (se `GOTENBERG_URL` faltar,
    degrada devolvendo o HTML, como as demais telas);
  - **registra** em `lgpd_solicitacao_titular` (tipo `acesso`, status `concluida`) — a prova do atendimento;
  - devolve o buffer/JSON para download.

- [ ] **Passo 3: Verificar e commitar**

```bash
npm run lint && npm run typecheck && npm test
git add src/lib/lgpd/relatorio.ts "src/app/(app)/lgpd/actions.ts"
git commit -m "feat(lgpd): relatorio de dados por titular em PDF e JSON, com registro"
```

---

### Tarefa 4: Anonimização + solicitação de exclusão

**Arquivos:** Modificar `src/app/(app)/lgpd/actions.ts`

- [ ] **Passo 1: `anonimizarTitular(clienteId)`** (gate admin), na ordem:
  1. lê os **sinais fiscais** do cliente (existe NFS-e? título? documento? obrigação? — uma query cada, com
     `service_role`) e a `data_saida`/`excluido_em`;
  2. `vereditoRetencao(...)` com `escritorio_config.retencao_meses`;
  3. **anonimiza** `clientes` nos `CAMPOS_CLIENTE_ANONIMIZAVEIS` (marcador) e desativa+anonimiza os usuários
     do portal do cliente. **Nunca** toca razão social, CNPJ, inscrições, títulos, NFS-e;
  4. grava `lgpd_solicitacao_titular` (tipo `exclusao`, status `concluida`, `retido` e `anonimizado` em jsonb);
  5. gera a **resposta documentada** (PDF via `converterPdfHtml`): o que foi anonimizado, o que foi retido e a
     base legal.
  - **Confirmação forte:** a action exige um parâmetro `confirmar: true` — anonimização não se desfaz.

- [ ] **Passo 2:** `abrirSolicitacao(clienteId, tipo)` — registra um pedido `aberta` (para quando o titular
  pede e a execução é posterior); a anonimização conclui.

- [ ] **Passo 3: Verificar e commitar**

```bash
npm run lint && npm run typecheck && npm test
git commit -am "feat(lgpd): anonimizacao do titular com trava de retencao fiscal e resposta documentada"
```

---

### Tarefa 5: Consentimento no histórico

**Arquivos:** Modificar `src/app/(app)/comunicados/actions.ts` (`setAceitaComunicados`); criar helper
`src/lib/lgpd/consentimento.ts`

- [ ] **Passo 1:** `registrarConsentimento(clienteId, tipo, concedido, origem, usuarioId)` — insere em
  `lgpd_consentimento_evento` via `createAdminSupabase()` (sem policy de INSERT).
- [ ] **Passo 2:** `setAceitaComunicados` passa a **também** gravar o evento (`tipo: 'comunicados'`,
  `concedido: aceita`, `origem: 'ficha'`, `usuarioId`). Hoje só muda o estado; agora deixa a prova histórica.
- [ ] **Passo 3: Verificar e commitar**

```bash
npm run lint && npm run typecheck && npm test
git commit -am "feat(lgpd): registra o historico de consentimento a cada mudanca de opt-in"
```

---

### Tarefa 6: Telas

**Arquivos:**
- Criar: `src/app/(app)/lgpd/page.tsx` (painel), `tratamentos/…` (CRUD ROPA), `FormConfigLgpd.tsx` (retenção + encarregado)
- Modificar: `src/app/(app)/configuracoes/page.tsx` (item "LGPD"); `src/app/(app)/clientes/[id]/page.tsx` (seção LGPD)
- Modificar: `0096`/seed — inserir `TRATAMENTOS_SEED` (via um passo idempotente na migration ou uma action de "restaurar padrão")

- [ ] **Passo 1: `/lgpd`** (admin) — três blocos: **Tratamentos** (ROPA: lista + CRUD, com botão "restaurar
  padrão" que semeia o `TRATAMENTOS_SEED` se estiver vazio), **Solicitações do titular** (histórico) e
  **Configuração** (retenção em meses + encarregado/DPO).
- [ ] **Passo 2: Ficha do cliente** — seção **LGPD** (admin): "Gerar relatório de dados" (PDF/JSON) e
  "Registrar pedido de exclusão" (abre confirmação forte antes de anonimizar).
- [ ] **Passo 3:** item **LGPD** em Configurações.
- [ ] **Passo 4: Verificar e commitar**

```bash
npm run lint && npm run typecheck && npm test && npm run build
git add -A && git commit -m "feat(lgpd): painel de ROPA, config de retencao e secao na ficha do cliente"
```

---

### Tarefa 7: Documentação, entrega e tag

- [ ] **Passo 1:** `docs/DOCUMENTACAO.md` (nova seção "LGPD" — o eixo honesto: anonimização respeita a
  retenção fiscal; ROPA pré-semeado; relatório PDF+JSON; consentimento com histórico) + `CHANGELOG.md`.
- [ ] **Passo 2:** Commit, merge `develop` → `main`, push.
- [ ] **Passo 3: Pedir ao usuário, explicitamente:** implantar e validar — conferir o ROPA semeado; gerar o
  relatório de um cliente (PDF + JSON) e ver que reúne cadastro/documentos/títulos/e-mails; registrar um
  pedido de exclusão num cliente **inativo e sem título recente** e confirmar que e-mail/telefone viram
  `[anonimizado]` mas razão social/CNPJ e os títulos permanecem, com a resposta documentada gerada.
- [ ] **Passo 4:** Após o "validei, deu certo": tag `v6.1.0`.
