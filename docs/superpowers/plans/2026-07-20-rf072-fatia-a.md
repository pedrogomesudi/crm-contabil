# RF-072 — Rentabilidade por segmento — Fatia A (regime) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Um seletor "Agrupar por: Cliente / Regime / Porte" na tela de Rentabilidade que soma os mesmos números por segmento — nesta fatia, agrupando por regime tributário (dado que já existe).

**Architecture:** Lógica pura `agruparRentabilidade` soma horas/custo/recebido/contratado por dimensão; a ação de rentabilidade passa a trazer o `regime` de cada cliente; a página (server component, form GET) renderiza a tabela agrupada e o export conforme o `agrupar` escolhido.

**Tech Stack:** Next.js 16 App Router (server component), TypeScript, Tailwind 4, vitest.

## Global Constraints

- Rentabilidade = `podeGerenciarFinanceiro`; roda via service_role. Sem schema nesta fatia (regime já existe).
- Guard `divida-ui`: sem `border` estático em input escrito à mão; sem `←`/`amber-\d`.
- Imports `@/*`. Rodar antes de commitar: `npm run lint && npm run typecheck && npm test && npm run format && npm run build`.

---

### Task 1: Lógica pura `agruparRentabilidade`

**Files:**
- Create: `src/lib/timesheet/segmento.ts`
- Test: `src/tests/timesheet/segmento.test.ts`

**Interfaces:**
- Produces:
  - `type GrupoRentab = { grupo: string; minutos: number; custo: number; recebido: number; contratado: number }`
  - `agruparRentabilidade(linhas, dimensao: "regime" | "porte"): GrupoRentab[]`

- [ ] **Step 1: Write the failing test**

```ts
// src/tests/timesheet/segmento.test.ts
import { describe, it, expect } from "vitest";
import { agruparRentabilidade } from "@/lib/timesheet/segmento";

const linhas = [
  { minutos: 60, custo: 10, recebido: 100, contratado: 50, regime: "Simples", porte: "ME" },
  { minutos: 120, custo: 20, recebido: 200, contratado: 80, regime: "Simples", porte: "EPP" },
  { minutos: 30, custo: 5, recebido: 50, contratado: 0, regime: "Presumido", porte: "ME" },
  { minutos: 0, custo: 0, recebido: 0, contratado: 0, regime: null, porte: null },
];

describe("agruparRentabilidade", () => {
  it("agrupa por regime, soma e ordena por recebido desc", () => {
    expect(agruparRentabilidade(linhas, "regime")).toEqual([
      { grupo: "Simples", minutos: 180, custo: 30, recebido: 300, contratado: 130 },
      { grupo: "Presumido", minutos: 30, custo: 5, recebido: 50, contratado: 0 },
      { grupo: "Não classificado", minutos: 0, custo: 0, recebido: 0, contratado: 0 },
    ]);
  });
  it("agrupa por porte; null vira 'Não classificado'", () => {
    const g = agruparRentabilidade(linhas, "porte");
    expect(g.map((x) => x.grupo)).toEqual(["ME", "EPP", "Não classificado"]);
    expect(g.find((x) => x.grupo === "ME")).toEqual({ grupo: "ME", minutos: 90, custo: 15, recebido: 150, contratado: 50 });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/tests/timesheet/segmento.test.ts`
Expected: FAIL (módulo não existe).

- [ ] **Step 3: Implement**

```ts
// src/lib/timesheet/segmento.ts
export type GrupoRentab = { grupo: string; minutos: number; custo: number; recebido: number; contratado: number };

type LinhaSeg = {
  minutos: number;
  custo: number;
  recebido: number;
  contratado: number;
  regime?: string | null;
  porte?: string | null;
};

export function agruparRentabilidade(linhas: LinhaSeg[], dimensao: "regime" | "porte"): GrupoRentab[] {
  const mapa = new Map<string, GrupoRentab>();
  for (const l of linhas) {
    const bruto = dimensao === "regime" ? l.regime : l.porte;
    const grupo = bruto && bruto.trim() !== "" ? bruto : "Não classificado";
    const g = mapa.get(grupo) ?? { grupo, minutos: 0, custo: 0, recebido: 0, contratado: 0 };
    g.minutos += l.minutos;
    g.custo += l.custo;
    g.recebido += l.recebido;
    g.contratado += l.contratado;
    mapa.set(grupo, g);
  }
  return [...mapa.values()].sort((a, b) => b.recebido - a.recebido);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/tests/timesheet/segmento.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/timesheet/segmento.ts src/tests/timesheet/segmento.test.ts
git commit -m "feat(rf072): agruparRentabilidade (lógica pura)"
```

---

### Task 2: `regime` em `LinhaRentab` + na ação

**Files:**
- Modify: `src/lib/timesheet/rentabilidade.ts` (tipo)
- Modify: `src/app/(app)/financeiro/rentabilidade/actions.ts` (select + map)

**Interfaces:**
- Produces: `LinhaRentab` ganha `regime: string | null`.

- [ ] **Step 1: Add `regime` to the type**

Em `src/lib/timesheet/rentabilidade.ts`, no tipo `LinhaRentab`, após `clienteNome`:

```ts
  regime: string | null;
```

- [ ] **Step 2: Fetch and map `regime`**

Em `src/app/(app)/financeiro/rentabilidade/actions.ts`:

(a) o select de `clientes` passa a incluir `regime_tributario`:

```ts
    .select("id, razao_social, regime_tributario, clientes_financeiro(honorario_mensal)")
```

(b) no `map` que monta `linhas`, adicionar o campo (após `clienteNome`):

```ts
      regime: (c.regime_tributario as string | null) ?? null,
```

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`
Expected: sem erros (o tipo novo é preenchido na ação; a página ainda não usa — ok).

- [ ] **Step 4: Commit**

```bash
git add src/lib/timesheet/rentabilidade.ts "src/app/(app)/financeiro/rentabilidade/actions.ts"
git commit -m "feat(rf072): traz o regime tributário em LinhaRentab"
```

---

### Task 3: Seletor + tabela agrupada na página

**Files:**
- Modify: `src/app/(app)/financeiro/rentabilidade/page.tsx`

**Interfaces:**
- Consumes: `agruparRentabilidade`, `GrupoRentab`, `margem`.

- [ ] **Step 1: Ler o `agrupar` e importar a agregação**

Em `src/app/(app)/financeiro/rentabilidade/page.tsx`:

(a) imports (junto aos existentes):

```tsx
import { agruparRentabilidade, type GrupoRentab } from "@/lib/timesheet/segmento";
```

(b) ampliar o tipo de `searchParams` e ler `agrupar`:

```tsx
  searchParams,
}: {
  searchParams: Promise<{ de?: string; ate?: string; agrupar?: string }>;
}) {
```

após `const ate = ...`:

```tsx
  const agrupar: "cliente" | "regime" | "porte" =
    sp.agrupar === "regime" || sp.agrupar === "porte" ? sp.agrupar : "cliente";
  const grupos: GrupoRentab[] = agrupar === "cliente" ? [] : agruparRentabilidade(rel.linhas, agrupar);
```

- [ ] **Step 2: Adicionar o seletor no form GET**

No `<form method="GET">`, após o campo "Até", inserir:

```tsx
        <label className="text-xs text-cinza">
          Agrupar por
          <select name="agrupar" defaultValue={agrupar} className={`${controleCls("compacto")} mt-0.5 block`}>
            <option value="cliente">Cliente</option>
            <option value="regime">Regime tributário</option>
            <option value="porte">Porte</option>
          </select>
        </label>
```

- [ ] **Step 3: Export conforme o agrupamento**

Trocar a montagem do `relatorio` para escolher entre por-cliente e por-grupo. Substituir a constante
`relatorio` por:

```tsx
  const relatorio: RelatorioExportavel =
    agrupar === "cliente"
      ? {
          titulo: "Rentabilidade por cliente",
          subtitulo: `${formatarData(de)} a ${formatarData(ate)}`,
          colunas: [
            { chave: "cliente", rotulo: "Cliente", formato: "texto" },
            { chave: "horas", rotulo: "Horas", formato: "texto" },
            { chave: "custo", rotulo: "Custo", formato: "moeda" },
            { chave: "recebido", rotulo: "Recebido", formato: "moeda" },
            { chave: "contratado", rotulo: "Contratado", formato: "moeda" },
            { chave: "margem", rotulo: "Margem", formato: "moeda" },
            { chave: "pct", rotulo: "%", formato: "percent" },
            { chave: "porHora", rotulo: "R$/hora", formato: "moeda" },
          ],
          linhas: rel.linhas.map((l) => {
            const m = margem(l);
            return {
              cliente: l.clienteNome,
              horas: formatarHoras(l.minutos),
              custo: l.custo,
              recebido: l.recebido,
              contratado: l.contratado,
              margem: m.valor,
              pct: m.pct,
              porHora: m.porHora,
            };
          }),
          totais: {
            cliente: "Total",
            horas: formatarHoras(rel.totais.minutos),
            custo: rel.totais.custo,
            recebido: rel.totais.recebido,
            contratado: rel.totais.contratado,
            margem: totalMargem,
            pct: null,
            porHora: null,
          },
        }
      : {
          titulo: `Rentabilidade por ${agrupar === "regime" ? "regime" : "porte"}`,
          subtitulo: `${formatarData(de)} a ${formatarData(ate)}`,
          colunas: [
            { chave: "grupo", rotulo: agrupar === "regime" ? "Regime" : "Porte", formato: "texto" },
            { chave: "horas", rotulo: "Horas", formato: "texto" },
            { chave: "custo", rotulo: "Custo", formato: "moeda" },
            { chave: "recebido", rotulo: "Recebido", formato: "moeda" },
            { chave: "contratado", rotulo: "Contratado", formato: "moeda" },
            { chave: "margem", rotulo: "Margem", formato: "moeda" },
            { chave: "pct", rotulo: "%", formato: "percent" },
          ],
          linhas: grupos.map((g) => {
            const m = margem(g);
            return {
              grupo: g.grupo,
              horas: formatarHoras(g.minutos),
              custo: g.custo,
              recebido: g.recebido,
              contratado: g.contratado,
              margem: m.valor,
              pct: m.pct,
            };
          }),
          totais: {
            grupo: "Total",
            horas: formatarHoras(rel.totais.minutos),
            custo: rel.totais.custo,
            recebido: rel.totais.recebido,
            contratado: rel.totais.contratado,
            margem: totalMargem,
            pct: null,
          },
        };
```

- [ ] **Step 4: Renderizar a tabela agrupada quando aplicável**

Envolver a tabela atual em `{agrupar === "cliente" && ( ...tabela por cliente... )}` e adicionar, ao lado,
o bloco da tabela agrupada `{agrupar !== "cliente" && ( ... )}`:

```tsx
      {agrupar !== "cliente" && (
        <div className="overflow-x-auto rounded-2xl border border-linha bg-white">
          <table className="min-w-full text-sm">
            <thead>
              <tr className="border-b border-linha text-xs text-cinza">
                <th className="px-3 py-2 text-left font-medium">{agrupar === "regime" ? "Regime" : "Porte"}</th>
                <th className="px-3 py-2 text-right font-medium">Horas</th>
                <th className="px-3 py-2 text-right font-medium">Custo</th>
                <th className="px-3 py-2 text-right font-medium">Recebido</th>
                <th className="px-3 py-2 text-right font-medium">Contratado</th>
                <th className="px-3 py-2 text-right font-medium">Margem</th>
                <th className="px-3 py-2 text-right font-medium">%</th>
              </tr>
            </thead>
            <tbody>
              {grupos.map((g) => {
                const m = margem(g);
                return (
                  <tr key={g.grupo} className="border-b border-linha/60 hover:bg-creme">
                    <td className="px-3 py-2 text-texto">{g.grupo}</td>
                    <td className="px-3 py-2 text-right text-cinza">{formatarHoras(g.minutos)}</td>
                    <td className="px-3 py-2 text-right text-cinza">{formatarMoeda(g.custo)}</td>
                    <td className="px-3 py-2 text-right text-texto">{formatarMoeda(g.recebido)}</td>
                    <td className="px-3 py-2 text-right text-cinza">{formatarMoeda(g.contratado)}</td>
                    <td className={`px-3 py-2 text-right font-medium ${m.valor < 0 ? "text-negativo" : "text-texto"}`}>
                      {formatarMoeda(m.valor)}
                    </td>
                    <td className={`px-3 py-2 text-right ${m.pct !== null && m.pct < 0 ? "text-negativo" : "text-cinza"}`}>
                      {m.pct === null ? "—" : `${m.pct}%`}
                    </td>
                  </tr>
                );
              })}
            </tbody>
            <tfoot>
              <tr className="border-t border-linha bg-creme text-sm font-medium">
                <td className="px-3 py-2 text-texto">Total</td>
                <td className="px-3 py-2 text-right text-texto">{formatarHoras(rel.totais.minutos)}</td>
                <td className="px-3 py-2 text-right text-texto">{formatarMoeda(rel.totais.custo)}</td>
                <td className="px-3 py-2 text-right text-texto">{formatarMoeda(rel.totais.recebido)}</td>
                <td className="px-3 py-2 text-right text-texto">{formatarMoeda(rel.totais.contratado)}</td>
                <td className={`px-3 py-2 text-right ${totalMargem < 0 ? "text-negativo" : "text-texto"}`}>
                  {formatarMoeda(totalMargem)}
                </td>
                <td />
              </tr>
            </tfoot>
          </table>
        </div>
      )}
```

E na tabela por cliente existente (o `<div className="overflow-x-auto ...">` que a contém), envolver com
`{agrupar === "cliente" && ( ... )}`.

- [ ] **Step 5: Full gate**

Run: `npm run lint && npm run typecheck && npm test && npm run build`
Expected: tudo verde.

- [ ] **Step 6: Commit**

```bash
git add "src/app/(app)/financeiro/rentabilidade/page.tsx"
git commit -m "feat(rf072): seletor de agrupamento e tabela por regime na rentabilidade"
```

---

## Self-Review

**1. Spec coverage (Fatia A):**
- `agruparRentabilidade` (soma por dimensão, null → "Não classificado", ordena por recebido) → Task 1. ✅
- `regime` em `LinhaRentab` + ação → Task 2. ✅
- Seletor Cliente/Regime/Porte + tabela agrupada + export por grupo → Task 3. ✅
- Porte no seletor já aparece (cai em "Não classificado" até a Fatia B) → Task 3. ✅

**2. Placeholder scan:** Nenhum TBD/TODO; código completo. ✅

**3. Type consistency:** `LinhaRentab` ganha `regime` (Task 2), consumido por `agruparRentabilidade` (Task 1) via campo opcional `regime?`; `GrupoRentab` tem os 4 numéricos + `grupo`, e `margem()` (que lê custo/recebido/minutos) aceita `GrupoRentab`. O `agrupar` é `"cliente"|"regime"|"porte"` consistente entre searchParams, seletor, export e tabela. ✅

**Nota:** a página é server component; o seletor vive no form GET (submete no "Aplicar"), sem client component — consistente com os campos de/até já existentes.
