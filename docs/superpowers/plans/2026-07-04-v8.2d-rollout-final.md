# V8.2d — Rollout SALDO Final Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Concluir o rollout SALDO nos 19 arquivos restantes (atendimento, integrações, documentos, assinatura, usuários, sobras), com zero resíduo de estilo antigo em `src`.

**Architecture:** Re-skin determinístico (mapa de tokens por sweep) + adoção de primitivos + helper novo `badgePapel`. Sem mudança de lógica/RLS/uploads/Clicksign.

**Tech Stack:** Next.js 16, Tailwind 4 (tokens SALDO), TypeScript, Vitest. Sem banco.

## Global Constraints

- **Só visual:** preservar `name`/`value`/`onChange`/actions/`aria`/`role`; uploads, Clicksign, convites, envio/polling **intactos**.
- **Mapa de tokens (das fatias anteriores):** `text-slate-900→text-texto`; `text-slate-700/600→text-cinza`; `text-slate-500/400→text-cinza-claro`; `border-slate-*→border-linha`; `divide-slate-100→divide-linha/70`; `bg-slate-100/50→bg-creme`; `hover:bg-slate-50→hover:bg-creme`; botão primário `bg-slate-900`→`bg-verde` (`rounded-lg … font-medium hover:brightness-105`); `text-red-600/700→text-negativo`; `bg-red-50→bg-negativo/10`; `text-green-700→text-verde`; `bg-green-50→bg-verde/10`; `bg-amber-*/text-amber-*`→manter; `bg-green-100`→`bg-verde` (badge de não-lidas); títulos `text-lg/2xl font-semibold`→`font-display … font-bold`; dados→`font-mono`.
- **Guia:** `docs/design/saldo-ui.md`. **Gate final:** zero resíduo slate em `src/app`+`src/components` (fora de `ui/`/`marca/`).
- **Antes de commitar:** `npm run lint && npm run typecheck && npm test && npm run build`.

---

## File Structure

**Modificar:**
- `src/lib/ui/apresentacao.ts` (+ `badgePapel`), `src/tests/ui/apresentacao.test.ts` (+ teste).
- Atendimento: `src/app/(app)/atendimento/{Inbox,page}.tsx`.
- Usuários: `src/app/(app)/usuarios/page.tsx`, `src/components/ConviteForm.tsx`.
- Domínio: `src/components/dominio/{AtualizarPelaReceita,ImportarEnderecos,PreviaImportacao,UploadDominio}.tsx`.
- Documentos: `src/components/documentos/{DocumentosSection,UploadDocumento,BotaoBaixar}.tsx`.
- Assinatura/Contrato: `src/components/assinatura/{EnviarAssinatura,StatusAssinatura}.tsx`, `src/components/contrato/GerarContrato.tsx`.
- Config WhatsApp: `src/app/(app)/configuracoes/whatsapp/{Formularios,page}.tsx`.
- Sobras: `src/app/(app)/page.tsx`, `src/app/login/recuperar/page.tsx`, `src/components/CardResumo.tsx`.

---

## Task 1: Helper `badgePapel`

**Files:**
- Modify: `src/lib/ui/apresentacao.ts`, `src/tests/ui/apresentacao.test.ts`

**Interfaces:**
- Produces: `badgePapel(papel: string): "ia" | "positivo" | "neutro" | "atencao"`.

- [ ] **Step 1: Teste (falha)**

Append em `src/tests/ui/apresentacao.test.ts`:

```ts
import { badgePapel } from "@/lib/ui/apresentacao";

describe("badgePapel", () => {
  it("mapeia o papel para a variante do Badge", () => {
    expect(badgePapel("admin")).toBe("ia");
    expect(badgePapel("financeiro")).toBe("positivo");
    expect(badgePapel("contador")).toBe("neutro");
    expect(badgePapel("assistente")).toBe("atencao");
    expect(badgePapel("qualquer")).toBe("neutro");
  });
});
```

- [ ] **Step 2: Rodar (falha)**

Run: `npm test -- src/tests/ui/apresentacao.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar**

Append em `src/lib/ui/apresentacao.ts`:

```ts
// Papel do usuário → variante de cor do Badge.
export function badgePapel(papel: string): "ia" | "positivo" | "neutro" | "atencao" {
  const p = (papel ?? "").toLowerCase();
  if (p === "admin") return "ia";
  if (p === "financeiro") return "positivo";
  if (p === "assistente") return "atencao";
  return "neutro"; // contador e desconhecidos
}
```

- [ ] **Step 4: Rodar (passa)**

Run: `npm test -- src/tests/ui/apresentacao.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/ui/apresentacao.ts src/tests/ui/apresentacao.test.ts
git commit -m "feat(ui): helper badgePapel (papel → variante Badge) (V8.2d)"
```

---

## Task 2: Atendimento (inbox de chat)

**Files:**
- Modify: `src/app/(app)/atendimento/Inbox.tsx`, `src/app/(app)/atendimento/page.tsx`

**Interfaces:**
- Consumes: `Painel`, `Botao`, `PageHeader` (existentes).

- [ ] **Step 1: `Inbox` (re-skin do chat)**

Em `src/app/(app)/atendimento/Inbox.tsx`: aplicar o mapa de tokens; lista de conversas (à esquerda) num `Painel` — item ativo `bg-creme`, prévia em `text-cinza-claro`, **badge de não-lidas** `bg-verde text-white` (era `bg-green-600`); thread (à direita) com balões — **saída** (`m.direcao === "OUT"`): `ml-auto bg-verde/15 text-texto`; **entrada**: `bg-creme text-texto`; caixa de resposta com input estilo SALDO + `Botao primario` "Enviar". Preservar polling, `listarConversas`/`abrirConversa`/`responder`.

- [ ] **Step 2: `atendimento/page` header**

Trocar o `<h1 …slate…>` por `font-display text-2xl font-bold tracking-tight text-texto` (ou `PageHeader`).

- [ ] **Step 3: Verificar**

Run: `npm run lint && npm run typecheck && npm run build 2>&1 | grep -E "atendimento|error TS|Compiled" | head -2`
Expected: sem erros; rota compila.

- [ ] **Step 4: Commit**

```bash
git add "src/app/(app)/atendimento/Inbox.tsx" "src/app/(app)/atendimento/page.tsx"
git commit -m "feat(ui): inbox de atendimento no estilo SALDO (balões + não-lidas) (V8.2d)"
```

---

## Task 3: Usuários + convite

**Files:**
- Modify: `src/app/(app)/usuarios/page.tsx`, `src/components/ConviteForm.tsx`

**Interfaces:**
- Consumes: `PageHeader`, `Painel`, `Badge`, `Botao`, `Campo`/`inputCls`; `badgePapel` (Task 1).

- [ ] **Step 1: `usuarios/page` (lista + badge de papel)**

Aplicar o mapa de tokens; `PageHeader` no topo; lista de usuários em `Painel` (tabela no padrão SALDO); coluna de papel → `<Badge variante={badgePapel(u.papel)}>{u.papel}</Badge>` (importar `Badge` + `badgePapel`); ações (ativar/desativar, reenviar convite) via `Botao`/links estilizados. Preservar gates/actions.

- [ ] **Step 2: `ConviteForm`**

Campos → `Campo`/`inputCls`/`Select`; botão → `Botao`; mensagens semânticas. Preservar `name`/action.

- [ ] **Step 3: Verificar**

Run: `npm run lint && npm run typecheck && npm run build 2>&1 | grep -E "usuarios|error TS|Compiled" | head -2`
Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add "src/app/(app)/usuarios/page.tsx" src/components/ConviteForm.tsx
git commit -m "feat(ui): usuários + convite no estilo SALDO (badge de papel) (V8.2d)"
```

---

## Task 4: Integrações, documentos, assinatura, config WhatsApp e sobras

**Files:**
- Modify: `src/components/dominio/{AtualizarPelaReceita,ImportarEnderecos,PreviaImportacao,UploadDominio}.tsx`, `src/components/documentos/{DocumentosSection,UploadDocumento,BotaoBaixar}.tsx`, `src/components/assinatura/{EnviarAssinatura,StatusAssinatura}.tsx`, `src/components/contrato/GerarContrato.tsx`, `src/app/(app)/configuracoes/whatsapp/{Formularios,page}.tsx`, `src/app/(app)/page.tsx`, `src/app/login/recuperar/page.tsx`, `src/components/CardResumo.tsx`

- [ ] **Step 1: Sweep do mapa de tokens**

Aplicar o mapa de tokens (Global Constraints) em todos os arquivos acima. Cobre a maior parte (cores, bordas, botões primários, títulos). Onde houver **lista/tabela** (prévia de importação, documentos) envolver em `Painel`; botões de ação (upload/baixar/atualizar/gerar/enviar) → `Botao` (baixar/secundário, perigo onde destrutivo).

- [ ] **Step 2: `CardResumo` legado**

Verificar consumidores: `grep -rn "CardResumo" src/`. Se ainda usado, re-skinnar (`Card` + `font-display` no valor); se sem uso, remover o arquivo e o import.

- [ ] **Step 3: Verificar**

Run: `npm run lint && npm run typecheck && npm test 2>&1 | grep "Tests " && npm run build 2>&1 | grep -E "error TS|Compiled" | head -1`
Expected: sem erros; testes passam; "Compiled successfully".

- [ ] **Step 4: Verificar no app**

`npm run dev` → Integração Domínio (upload/prévia/atualizar Receita), documentos (na ficha), config WhatsApp, recuperação de senha. **Testar** um upload de documento e a prévia de importação (funcionam).
Expected: visual SALDO, zero regressão.

- [ ] **Step 5: Commit**

```bash
git add src/components/dominio src/components/documentos src/components/assinatura src/components/contrato/GerarContrato.tsx "src/app/(app)/configuracoes/whatsapp" "src/app/(app)/page.tsx" "src/app/login/recuperar/page.tsx" src/components/CardResumo.tsx
git commit -m "feat(ui): integrações/documentos/assinatura/whatsapp-config + sobras no estilo SALDO (V8.2d)"
```

---

## Task 5: Fechamento do V8.2d (gate final)

**Files:** —

- [ ] **Step 1: Suíte completa**

Run: `npm run lint && npm run typecheck && npm test && npm run build`
Expected: tudo verde; build ok.

- [ ] **Step 2: GATE FINAL — zero resíduo em toda a plataforma**

Run: `grep -rlE "slate-[0-9]|bg-green-50|bg-red-50|bg-blue-|border-slate" src/app src/components 2>/dev/null | grep -vE "ui/|marca/" || echo "ZERO RESÍDUO — rollout completo"`
Expected: `ZERO RESÍDUO — rollout completo`. Se aparecer arquivo, corrigir e repetir.

- [ ] **Step 3: CHANGELOG, ROADMAP e memória**

Registrar a V8.2d no `CHANGELOG.md`; marcar V8.2d ✅ e **V8.2 (rollout) + V8 concluídas** em `ROADMAP.md`. Memória (`saldo-design-system`): rollout completo (todas as áreas), `badgePapel`, gate de zero resíduo.

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md ROADMAP.md
git commit -m "docs: V8.2d — rollout SALDO completo (V8 concluída)"
```

---

## Self-Review (feito na escrita)

**Cobertura do spec:** helper `badgePapel` (§4)→T1; atendimento (§4)→T2; usuários (§4)→T3; domínio/documentos/assinatura/whatsapp-config/sobras (§2/§4/§6)→T4; testes + gate final (§5)→T1/T5. Os 19 arquivos cobertos (atendimento×2→T2; usuários×2→T3; demais 15→T4/T1). ✅

**Placeholders:** re-skin por **recipe + mapa de tokens** (determinístico); código real no helper (T1). Sem "TBD".

**Consistência de tipos:** `badgePapel` (T1) retorna variantes existentes do `Badge` e é usado em T3; primitivos já existem; `CardResumo` tratado condicionalmente (re-skin ou remoção conforme uso — T4 Step 2). Gate de varredura em T5 garante cobertura total.
