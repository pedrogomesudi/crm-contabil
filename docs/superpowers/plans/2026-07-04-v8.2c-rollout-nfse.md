# V8.2c — Rollout SALDO (NFS-e) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aplicar a linguagem de design SALDO às 13 telas de NFS-e, reusando primitivos e o mapa de tokens.

**Architecture:** Re-skin determinístico (mapa de tokens por sweep) + adoção de primitivos (`Painel`/`Badge`/`Botao`/`Campo`/`Input`/`PageHeader`) + helper novo `badgeStatusNfse`. Sem mudança de lógica/RLS/mTLS. Emissão avulsa preservada.

**Tech Stack:** Next.js 16, Tailwind 4 (tokens SALDO), TypeScript, Vitest. Sem banco.

## Global Constraints

- **Só visual:** preservar `name`/`value`/`onChange`/actions/`aria`/`role`; **emissão avulsa e em lote não podem quebrar**.
- **Mapa de tokens (das fatias anteriores):** `text-slate-900→text-texto`; `text-slate-700/600→text-cinza`; `text-slate-500/400→text-cinza-claro`; `border-slate-*→border-linha`; `divide-slate-100→divide-linha/70`; `bg-slate-100/50→bg-creme`; `hover:bg-slate-50→hover:bg-creme`; botão primário `bg-slate-900`→`bg-verde` (`rounded-lg … font-medium hover:brightness-105`); `text-red-600/700→text-negativo`; `bg-red-50→bg-negativo/10`; `text-green-700→text-verde`; `bg-green-50→bg-verde/10`; `bg-amber-*/text-amber-*`→manter; títulos `text-2xl/lg font-semibold`→`font-display … font-bold`; número/CNPJ/valores→`font-mono`.
- **Guia:** `docs/design/saldo-ui.md`. **Semântica NFS-e:** autorizada=verde, cancelada=neutro, rejeitada=negativo, processando=âmbar.
- **Antes de commitar:** `npm run lint && npm run typecheck && npm test && npm run build`.

---

## File Structure

**Modificar:**
- `src/lib/ui/apresentacao.ts` (+ `badgeStatusNfse`), `src/tests/ui/apresentacao.test.ts` (+ teste).
- `src/components/nfse/{NotasFiscaisSection,EmissaoClienteSection,EmitirNfseCliente,EmitirNfse,LoteNfse,BaixarNfse,BaixarNotasZip,CancelarNfse,EmitenteConfig}.tsx`.
- `src/app/(app)/nfse/lote/page.tsx`, `src/app/(app)/configuracoes/nfse/{page,Formularios}.tsx`.

---

## Task 1: Helper `badgeStatusNfse`

**Files:**
- Modify: `src/lib/ui/apresentacao.ts`, `src/tests/ui/apresentacao.test.ts`

**Interfaces:**
- Produces: `badgeStatusNfse(status: string): "positivo" | "neutro" | "negativo" | "atencao"`.

- [ ] **Step 1: Teste (falha)**

Append em `src/tests/ui/apresentacao.test.ts`:

```ts
import { badgeStatusNfse } from "@/lib/ui/apresentacao";

describe("badgeStatusNfse", () => {
  it("mapeia o status da nota para a variante do Badge (case-insensitive)", () => {
    expect(badgeStatusNfse("autorizada")).toBe("positivo");
    expect(badgeStatusNfse("AUTORIZADA")).toBe("positivo");
    expect(badgeStatusNfse("cancelada")).toBe("neutro");
    expect(badgeStatusNfse("rejeitada")).toBe("negativo");
    expect(badgeStatusNfse("erro")).toBe("negativo");
    expect(badgeStatusNfse("processando")).toBe("atencao");
    expect(badgeStatusNfse("pendente")).toBe("atencao");
    expect(badgeStatusNfse("qualquer")).toBe("neutro");
  });
});
```

- [ ] **Step 2: Rodar (falha)**

Run: `npm test -- src/tests/ui/apresentacao.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar**

Append em `src/lib/ui/apresentacao.ts`:

```ts
// Status da NFS-e → variante de cor do Badge.
export function badgeStatusNfse(status: string): "positivo" | "neutro" | "negativo" | "atencao" {
  const s = (status ?? "").toLowerCase();
  if (s.includes("autorizada")) return "positivo";
  if (s.includes("rejeit") || s.includes("erro") || s.includes("falha")) return "negativo";
  if (s.includes("process") || s.includes("pendente") || s.includes("enviada")) return "atencao";
  return "neutro"; // cancelada e desconhecidos
}
```

- [ ] **Step 4: Rodar (passa)**

Run: `npm test -- src/tests/ui/apresentacao.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/ui/apresentacao.ts src/tests/ui/apresentacao.test.ts
git commit -m "feat(ui): helper badgeStatusNfse (status da nota → variante Badge) (V8.2c)"
```

---

## Task 2: Componentes de NFS-e (emissão + notas + lote)

**Files:**
- Modify: `src/components/nfse/{NotasFiscaisSection,EmissaoClienteSection,EmitirNfseCliente,EmitirNfse,LoteNfse,BaixarNfse,BaixarNotasZip,CancelarNfse}.tsx`, `src/app/(app)/nfse/lote/page.tsx`

**Interfaces:**
- Consumes: `Painel`, `Badge`, `Botao`, `Campo`/`Input`/`Select`, `PageHeader`; `badgeStatusNfse` (Task 1); `formatarMoeda` (existente).

- [ ] **Step 1: Sweep do mapa de tokens nos componentes**

Aplicar o mapa de tokens (Global Constraints) via substituição nos 8 componentes + a page de lote. Cobre a maior parte (cores, bordas, botões primários).

- [ ] **Step 2: Status da nota → `Badge`**

Onde o status da nota é exibido (`NotasFiscaisSection`, `LoteNfse`, e onde houver `status` de nota), envolver com `<Badge variante={badgeStatusNfse(status)}>{status}</Badge>` (importar `Badge` e `badgeStatusNfse`). Onde há lista de notas, envolver o contêiner em `Painel` e pôr **número/valor em `font-mono`** à direita.

- [ ] **Step 3: Botões e formulários**

Botões: emitir → `Botao primario`; cancelar → `Botao perigo` (manter confirmação); baixar PDF/XML/ZIP → `Botao secundario`/`fantasma`. Campos de emissão (avulsa e por competência) → `Campo`/`Input`/`Select`. **Preservar** todos os handlers/actions (emissão avulsa, por honorário, lote, cancelamento, ZIP).

- [ ] **Step 4: Verificar**

Run: `npm run lint && npm run typecheck && npm run build 2>&1 | grep -E "nfse|error TS|Compiled" | head -3`
Expected: sem erros; rotas compilam.

- [ ] **Step 5: Verificar no app**

`npm run dev` → ficha de cliente (seção de notas) e `/nfse/lote`: listas com badges de status, valores mono, botões SALDO. **Testar** que a emissão avulsa e a seleção de lote ainda funcionam.
Expected: visual SALDO, emissão intacta.

- [ ] **Step 6: Commit**

```bash
git add src/components/nfse "src/app/(app)/nfse/lote/page.tsx"
git commit -m "feat(ui): componentes de NFS-e no estilo SALDO (notas/emissão/lote) (V8.2c)"
```

---

## Task 3: Configuração do NFS-e (emitente + certificado)

**Files:**
- Modify: `src/components/nfse/EmitenteConfig.tsx`, `src/app/(app)/configuracoes/nfse/{page,Formularios}.tsx`

**Interfaces:**
- Consumes: `PageHeader`, `Card`, `Campo`/`Input`/`Select`, `Botao`; `inputCls` (`@/components/ui/Campo`).

- [ ] **Step 1: `Formularios` (substituir o `inputCls` local)**

Em `src/app/(app)/configuracoes/nfse/Formularios.tsx`, remover o `const inputCls = "…slate…"` local e importar `inputCls` de `@/components/ui/Campo` (classe SALDO). Aplicar o mapa de tokens no restante; botões → `Botao`.

- [ ] **Step 2: `configuracoes/nfse/page` + `EmitenteConfig`**

`PageHeader` no topo da page; seções (dados do emitente, certificado) em `Card`; campos com `Campo`/`Input`; aplicar o mapa de tokens; avisos semânticos (sucesso `verde/10`, erro `negativo/10`, alerta amber mantém). Preservar gates/actions (upload de certificado, salvar emitente).

- [ ] **Step 3: Verificar**

Run: `npm run lint && npm run typecheck && npm run build 2>&1 | grep -E "configuracoes/nfse|error TS|Compiled" | head -2`
Expected: sem erros; rota compila.

- [ ] **Step 4: Commit**

```bash
git add src/components/nfse/EmitenteConfig.tsx "src/app/(app)/configuracoes/nfse/page.tsx" "src/app/(app)/configuracoes/nfse/Formularios.tsx"
git commit -m "feat(ui): configuração de NFS-e no estilo SALDO (V8.2c)"
```

---

## Task 4: Fechamento do V8.2c

**Files:** —

- [ ] **Step 1: Suíte completa**

Run: `npm run lint && npm run typecheck && npm test && npm run build`
Expected: tudo verde; build ok.

- [ ] **Step 2: Varredura de resíduo**

Run: `grep -rlE "slate-[0-9]|bg-green-50|bg-red-50|bg-blue-" src/components/nfse "src/app/(app)/nfse" "src/app/(app)/configuracoes/nfse" 2>/dev/null || echo "limpo"`
Expected: `limpo` (ou só ocorrências justificadas). Corrigir sobras.

- [ ] **Step 3: CHANGELOG, ROADMAP e memória**

Registrar a V8.2c no `CHANGELOG.md`; marcar V8.2c ✅ em `ROADMAP.md`. Memória (`saldo-design-system`): NFS-e migrado, `badgeStatusNfse`, falta V8.2d (atendimento/integrações/documentos/assinatura/usuários).

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md ROADMAP.md
git commit -m "docs: V8.2c — rollout SALDO (NFS-e) entregue"
```

---

## Self-Review (feito na escrita)

**Cobertura do spec:** helper `badgeStatusNfse` (§4)→T1; componentes de notas/emissão/lote (§5)→T2; config emitente/certificado (§5)→T3; testes/varredura/regressão (§6)→T1/T2/T4. Os 13 arquivos cobertos (9 componentes+lote page→T2; EmitenteConfig+config page+Formularios→T3). ✅

**Placeholders:** re-skin por **recipe + mapa de tokens** (determinístico); código real no helper (T1). Sem "TBD".

**Consistência de tipos:** `badgeStatusNfse` (T1) retorna variantes existentes do `Badge` e é usado em T2; primitivos já existem (V8.1/V8.2a); `inputCls` de `@/components/ui/Campo` já existe. Regressão da **emissão avulsa** verificada no Step 5 da T2.
